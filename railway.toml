[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["app/**", "frontend/**", "alembic/**", "scripts/**", "mcp_server/**", "requirements.txt", "Dockerfile", "railway.toml", ".dockerignore"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
