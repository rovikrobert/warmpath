__pycache__/
*.py[cod]
*$py.class
*.so
.env
.env.dev
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
*.db
*.sqlite3
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Agent system artifacts (auto-generated)
agents/reports/*.json
agents/shared/intel_cache.json
agents/*/state.json

# CoS runtime artifacts
agents/chief_of_staff/cos_state.json
agents/chief_of_staff/notion_state.json
agents/chief_of_staff/reports/messages/*.txt

# Data team artifacts (auto-generated)
data_team/reports/*.json
!data_team/reports/.gitkeep
data_team/shared/intel_cache.json
data_team/*/state.json

# Product team artifacts (auto-generated)
product_team/reports/*.json
!product_team/reports/.gitkeep
product_team/shared/intel_cache.json
product_team/*/state.json

# Ops team artifacts (auto-generated)
ops_team/reports/*.json
!ops_team/reports/.gitkeep
ops_team/shared/intel_cache.json
ops_team/*/state.json

# Finance team artifacts (auto-generated)
finance_team/reports/*.json
!finance_team/reports/.gitkeep
finance_team/shared/intel_cache.json
finance_team/*/state.json
finance_team/cross_team_requests/*.json

# GTM team artifacts (auto-generated)
gtm_team/reports/*.json
!gtm_team/reports/.gitkeep
gtm_team/shared/intel_cache.json
gtm_team/*/state.json
gtm_team/cross_team_requests/*.json
# Shared agent caches
agents/shared/.web_cache/
agents/shared/shared_insights.json
agents/shared/resolved_registry.json

coverage.json
.coverage
junit.xml

# Mutation testing backup journals (safety restore files)
*.mutation_backup

# Claude Code local config
.claude/settings.json
.claude/agent-memory/

# Node (root-level only — frontend/ has its own package.json)
node_modules/
/package-lock.json
/package.json

# Generated reports (binary)
finance_team/reports/*.xlsx

# CoS telegram reports
agents/chief_of_staff/reports/telegram/*.txt

# User documents (not code)
*.docx

# One-off spec files
finance_team_spec.json

# Git worktrees
.worktrees/

# Ruler backup files
AGENTS.md.bak
CLAUDE.md.bak
